I have a very basic code editor connected to an llm that can make changes here.

So far the llm only edits one file and cannot execute code.

There is still a lot of stuff that has to happen.
add create file and create folder functionalities.
Decide how we can allow the llm to do more things and eventually execute code without breaking my system (some sort of sandboxing).
The "code editor" can currently only display one fixed file.

I also have to improve the chat view such that it can show all the things the llm can see insetad of dispalying something idnependent.
Which / how amny fiels were read is interesting for example + system prompt dispalyed at the top / via button.

I also want to set up some RL / BC once I use it mroe and get failed / bad executions.

I want to teach my llm to create a list where it searches the internet for jobs that fit my skills derived from my cv.
It has to check the location + skill overlap and seniority.
+ other things I might find.

It should then create a table where it adds a link + company name for fitting jobs without creating duplicate entries.
